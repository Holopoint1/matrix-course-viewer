# Never commit secrets. The service-role key lives here, locally only.
.env
.env.*
seed/node_modules/

# Regenerable read-only snapshot (run `npm run export` any time).
# Force-add a copy if you want a point-in-time versioned backup in git.
export/
